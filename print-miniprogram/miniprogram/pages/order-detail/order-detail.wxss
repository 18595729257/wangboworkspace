/* pages/order-detail/order-detail.wxss */

/* 状态横幅 */
.status-banner {
  display: flex;
  align-items: center;
  padding: 40rpx 32rpx;
  color: #ffffff;
  border-radius: var(--radius-lg);
  margin-bottom: 24rpx;
}

.status-banner.status-pending { background: linear-gradient(160deg, #FF9500 0%, #E08600 100%); }
.status-banner.status-paid { background: linear-gradient(160deg, #007AFF 0%, #0055D4 100%); }
.status-banner.status-printing { background: linear-gradient(160deg, #AF52DE 0%, #8944AB 100%); }
.status-banner.status-completed { background: linear-gradient(160deg, #34C759 0%, #2DA44E 100%); }
.status-banner.status-cancelled { background: linear-gradient(160deg, #8E8E93 0%, #636366 100%); }

.status-icon {
  font-size: 56rpx;
  margin-right: 22rpx;
}

.status-text {
  font-size: 34rpx;
  font-weight: 600;
}

/* 信息列表 */
.info-item {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: 16rpx 0;
  border-bottom: 1rpx solid var(--separator);
}

.info-item:last-child { border-bottom: none; }

.info-label {
  font-size: 28rpx;
  color: var(--label-secondary);
}

.info-value {
  font-size: 28rpx;
  color: var(--label);
}

/* 文件卡片 */
.file-card {
  display: flex;
  align-items: center;
  padding: 24rpx;
  background: var(--bg);
  border-radius: var(--radius-sm);
}

.file-icon {
  font-size: 44rpx;
  margin-right: 18rpx;
}

.file-info { flex: 1; }

.file-name {
  font-size: 28rpx;
  font-weight: 500;
  color: var(--label);
  margin-bottom: 4rpx;
  word-break: break-all;
}

.file-setting {
  font-size: 22rpx;
  color: var(--label-secondary);
}

/* 价格 */
.price-row {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: 12rpx 0;
}

.price-label {
  font-size: 28rpx;
  color: var(--label-secondary);
}

.price-value {
  font-size: 28rpx;
  color: var(--label);
}

.price-row.discount .price-value { color: var(--green); }

.price-row.total .price-label {
  font-size: 32rpx;
  font-weight: 600;
  color: var(--label);
}

/* 操作 */
.action-section {
  margin-top: 40rpx;
  padding-bottom: constant(safe-area-inset-bottom);
  padding-bottom: env(safe-area-inset-bottom);
}

.action-section button {
  margin-bottom: 18rpx;
}

.action-section button:last-child { margin-bottom: 0; }

.loading {
  display: flex;
  justify-content: center;
  align-items: center;
  height: 400rpx;
  color: var(--label-secondary);
  font-size: 28rpx;
}
